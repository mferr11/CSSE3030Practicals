# Compiled classes
*.class
.gradle/

# Randoop (downloaded jar + generated test output - students generate their own)
libs/
randoop-all-*.jar
RegressionTest*.java
ErrorTest*.java
.week4-tools/
week4-results/

# Java PathFinder / Symbolic PathFinder
# jpf-core & jpf-symbc are committed pre-built under jpf/ so students don't have
# to install/build them. Machine-specific site.properties files must not be
# committed. The two .git/ entries guard against nested-repo remnants.
jpf/site.properties
jpf/jpf-core/.git/
jpf/jpf-symbc/.git/
# Stray duplicate clone left over from setting this up - locked by the OS,
# harmless, safe to ignore (or delete manually once the lock clears)
/jpf-core/

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db
